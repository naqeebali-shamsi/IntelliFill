# IntelliFill Docker Environment Configuration
#
# Copy this file to .env.docker and fill in the values:
#   cp .env.docker.example .env.docker
#
# Then start Docker Compose with:
#   docker-compose --env-file .env.docker up

# ==================== Required Variables ====================
# These MUST be set or Docker Compose will fail to start

# PostgreSQL password (choose a strong password)
POSTGRES_PASSWORD=your_secure_password_here

# JWT secrets (use long random strings, e.g., openssl rand -base64 32)
JWT_SECRET=your_jwt_secret_here_min_32_chars
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_min_32_chars

# ==================== Optional Variables ====================
# These have sensible defaults but can be customized

# Environment
NODE_ENV=development
LOG_LEVEL=debug

# PostgreSQL
POSTGRES_DB=intellifill
POSTGRES_USER=intellifill
POSTGRES_PORT=5432

# Service Ports
BACKEND_PORT=3002
FRONTEND_PORT=8080

# Frontend Configuration
VITE_API_URL=http://localhost:3002/api
VITE_USE_BACKEND_AUTH=true

# ==================== Supabase (Optional) ====================
# Only needed if using Supabase authentication
# Leave empty to use backend-only authentication

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
