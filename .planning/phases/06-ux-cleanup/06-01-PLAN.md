# Plan 06-01: Critical UX Fixes

## Context

**Phase**: 6 - UX Cleanup
**Requirements**: UX-01, UX-02, UX-03, UX-04, UX-06, UX-07, UX-10
**Goal**: Remove broken/fake UI elements and surface hidden actions

## Prerequisites

- Frontend running (`cd quikadmin-web && bun run dev`)
- Understanding of current component structure

## Tasks

```xml
<task id="06-01-01" title="Remove non-functional search bar" requirement="UX-01">
  <context>
    AppLayout.tsx has a Search icon in the header that does nothing when clicked.
    This damages user trust - remove it entirely until real search is implemented.
  </context>
  <files>
    <file path="src/components/layout/AppLayout.tsx" action="modify">
      Remove Search icon import and usage from header area
    </file>
  </files>
  <verification>
    - Header no longer shows search icon
    - No console errors
  </verification>
</task>

<task id="06-01-02" title="Fix View button in FilledFormHistory" requirement="UX-02">
  <context>
    The Eye icon button in FilledFormHistory table does nothing when clicked.
    Either implement proper view functionality or remove the button.
  </context>
  <files>
    <file path="src/pages/FilledFormHistory.tsx" action="modify">
      Option A: Implement onView handler to open form details dialog/page
      Option B: Remove the View button entirely if viewing not supported
      Recommend Option A - navigate to form detail view
    </file>
  </files>
  <verification>
    - Click Eye button → navigates to form detail or opens modal
    - Or: Eye button removed from table actions
  </verification>
</task>

<task id="06-01-03" title="Remove fake status filters" requirement="UX-03">
  <context>
    FilledFormHistory has Draft/Completed/Submitted filter dropdown that doesn't
    actually filter the data. Remove until backend filtering is implemented.
  </context>
  <files>
    <file path="src/pages/FilledFormHistory.tsx" action="modify">
      Remove statusOptions array and Select dropdown for status filtering
      Keep the search input for name/form filtering if it works
    </file>
  </files>
  <verification>
    - No status filter dropdown visible
    - Page still displays form history correctly
  </verification>
</task>

<task id="06-01-04" title="Conditionally show Company Slug field" requirement="UX-04">
  <context>
    Login/Register pages show Company Slug field which confuses B2C users.
    Only show for multi-tenant/B2B mode (check for VITE_MULTI_TENANT flag or similar).
  </context>
  <files>
    <file path="src/pages/auth/Login.tsx" action="modify">
      Conditionally render Company Slug field based on tenant mode
    </file>
    <file path="src/pages/auth/Register.tsx" action="modify">
      Same conditional rendering for registration
    </file>
  </files>
  <verification>
    - In B2C mode: Company Slug field hidden
    - In B2B mode: Company Slug field visible
  </verification>
</task>

<task id="06-01-05" title="Surface Use Template action" requirement="UX-06">
  <context>
    TemplateCard has primary "Use Template" action buried in dropdown menu.
    This should be a prominent visible button since it's the main action.
  </context>
  <files>
    <file path="src/components/features/TemplateCard.tsx" action="modify">
      Add visible "Use" or "Use Template" button to card
      Keep dropdown for secondary actions (Edit, Duplicate, Delete)
    </file>
  </files>
  <verification>
    - Each template card shows visible "Use" button
    - Button triggers template usage flow
  </verification>
</task>

<task id="06-01-06" title="Remove duplicate Quick Actions panel" requirement="UX-07">
  <context>
    ConnectedDashboard has Quick Actions panel with links that duplicate
    the sidebar navigation. This adds visual clutter without value.
  </context>
  <files>
    <file path="src/pages/ConnectedDashboard.tsx" action="modify">
      Remove Quick Actions section/component from dashboard
      Keep other dashboard content (stats, recent activity, etc.)
    </file>
  </files>
  <verification>
    - Dashboard no longer shows Quick Actions panel
    - Dashboard still shows useful content (stats, recent items)
  </verification>
</task>

<task id="06-01-07" title="Simplify Documents page stats" requirement="UX-10">
  <context>
    DocumentLibrary page has 6-card stats dashboard that dominates the page.
    Users come to manage documents, not view metrics. Simplify or collapse.
  </context>
  <files>
    <file path="src/pages/DocumentLibrary.tsx" action="modify">
      Option A: Remove DocumentStatistics component entirely
      Option B: Make it collapsible, collapsed by default
      Option C: Reduce to single summary line (e.g., "12 documents, 3 pending")
      Recommend Option C for balance of info vs. space
    </file>
  </files>
  <verification>
    - Stats no longer dominate page
    - Document list is primary focus
  </verification>
</task>
```

## Verification

After all tasks:

1. **Visual check**: Header clean (no search), dashboard clean (no Quick Actions)
2. **Functional check**: View button works or removed, filters removed
3. **Auth check**: Company Slug hidden in B2C mode
4. **Template check**: "Use" button visible on template cards
5. **Documents check**: Stats minimized, document list prominent

## Notes

- All tasks are independent - can be done in any order
- Each task is small (~5-15 lines changed)
- Run `bun run typecheck` after changes to catch type errors
