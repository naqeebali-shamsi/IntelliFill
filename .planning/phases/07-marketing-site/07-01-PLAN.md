---
phase: 07-marketing-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quikadmin-web/src/pages/Login.tsx
  - quikadmin-web/src/pages/Register.tsx
autonomous: true
---

<objective>
Remove marketing content from authentication pages to create clean, focused login/register experiences.

Purpose: Auth pages should be simple and fast-loading. Marketing content belongs on the dedicated marketing site (Phase 07-02).
Output: Streamlined auth pages without hero sections, testimonials, or feature highlights.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 6 established patterns for UI cleanup
@.planning/phases/06-ux-cleanup/06-01-SUMMARY.md

# Current auth pages with marketing content
@quikadmin-web/src/pages/Login.tsx
@quikadmin-web/src/pages/Register.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove marketing content from Login page</name>
  <files>quikadmin-web/src/pages/Login.tsx</files>
  <action>
    Remove the following marketing elements from Login.tsx:

    1. Remove the entire left-side hero section (lines ~133-161):
       - The desktop-only marketing panel with logo, headline, feature highlights, and testimonial carousel
       - Remove imports: Testimonial, Boxes (background), Zap/Shield/Clock icons used for features
       - Remove FeatureHighlight component and its usage

    2. Remove mobile features banner (lines ~409-424):
       - The "Why IntelliFill?" section with icon grid
       - Remove MobileFeature component and its usage

    3. Simplify the layout:
       - Change from split-screen (lg:w-1/2) to centered single-column layout
       - Keep the animated background (Boxes) as subtle visual interest, or remove if too heavy
       - Center the login card with max-w-md on all screen sizes

    4. Keep essential elements:
       - AnimatedLogo (centered at top)
       - Login form with all inputs and validation
       - OAuth buttons (Google)
       - Demo login button (if VITE_ENABLE_DEMO)
       - Links (forgot password, register)

    Result: A clean, fast-loading login page focused on authentication.
  </action>
  <verify>
    - bun run build succeeds in quikadmin-web
    - Login page renders without errors at localhost:8080/login
    - No unused imports or dead code
  </verify>
  <done>
    Login page loads without marketing content, form is centered, all auth functionality preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove marketing content from Register page</name>
  <files>quikadmin-web/src/pages/Register.tsx</files>
  <action>
    Apply the same cleanup pattern to Register.tsx:

    1. Examine Register.tsx structure (likely similar to Login.tsx)

    2. Remove marketing elements:
       - Hero/feature sections if present
       - Testimonial carousel if present
       - Feature highlights if present
       - Mobile marketing banner if present

    3. Simplify layout:
       - Center the registration form
       - Keep AnimatedLogo at top
       - Keep all form fields and validation
       - Keep OAuth buttons
       - Keep link to login page

    4. Clean up:
       - Remove unused imports
       - Remove unused helper components

    Note: If Register.tsx doesn't have marketing content, document this and mark task complete.
  </action>
  <verify>
    - bun run build succeeds in quikadmin-web
    - Register page renders without errors at localhost:8080/register
    - No unused imports or dead code
  </verify>
  <done>
    Register page is clean and focused, matching Login page style. All registration functionality preserved.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd quikadmin-web && bun run build` succeeds without errors
- [ ] `cd quikadmin-web && bun run typecheck` passes
- [ ] Login page at /login shows only auth form (no marketing)
- [ ] Register page at /register shows only registration form (no marketing)
- [ ] All auth functionality works (login, register, OAuth, demo)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Auth pages load significantly faster (no carousel, fewer assets)
- No visual regressions in auth functionality
- Requirement UX-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-marketing-site/07-01-SUMMARY.md`
</output>
