# Force installation of optional dependencies (required for sharp)
omit=[]
optional=true

# Allow installation even with Node version mismatches
engine-strict=false

# Use public npm registry
registry=https://registry.npmjs.org/
