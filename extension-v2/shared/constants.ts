/** Default API base URL (production) */
export const DEFAULT_API_URL = 'http://localhost:3002/api';

/** Profile cache duration in milliseconds (5 minutes) */
export const CACHE_DURATION_MS = 5 * 60 * 1000;

/** Debounce delay for input events (milliseconds) */
export const DEBOUNCE_DELAY_MS = 300;

/** Maximum autocomplete suggestions to show */
export const MAX_SUGGESTIONS = 5;

/** Profile refresh interval (minutes) for chrome.alarms */
export const PROFILE_REFRESH_MINUTES = 5;
