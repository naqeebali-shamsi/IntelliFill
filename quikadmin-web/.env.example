# ==========================================
# QuikAdmin Frontend Environment Configuration
# ==========================================
# AUTHORITATIVE SOURCE for: VITE_API_URL, VITE_SUPABASE_*, VITE_* flags
#
# Copy this file to .env and fill in the actual values
#
# NOTE: All frontend vars must be prefixed with VITE_ to be accessible in browser

# API Configuration
VITE_API_URL=http://localhost:3002/api

# Supabase Configuration
# Get these values from your Supabase project dashboard (Settings > API)
# Create a project at: https://app.supabase.com
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Notes:
# - VITE_SUPABASE_URL: Your Supabase project URL
# - VITE_SUPABASE_ANON_KEY: Public anon key (safe for frontend, starts with "eyJ...")
# - NEVER use the service_role key on the frontend!
# - All Vite env vars must be prefixed with VITE_ to be accessible in the browser

# ==========================================
# Feature Flags
# ==========================================
# Enable multi-tenant (B2B) mode - shows Company Slug field on login
# Default: false (B2C mode, single-tenant)
# VITE_MULTI_TENANT=false

# Enable demo mode - shows demo login button
# VITE_ENABLE_DEMO=false

# ==========================================
# Error Monitoring (optional, production)
# ==========================================
# Sentry DSN for error tracking - get from https://sentry.io
# VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# VITE_APP_VERSION=1.0.0

# ==========================================
# E2E Testing (optional)
# ==========================================
# Only needed for password restoration in tests
# This key is used by E2E tests to reset user passwords after login tests
# Get from Supabase dashboard: Settings > API > service_role key
# SUPABASE_SERVICE_ROLE_KEY=eyJ...  # Required for password reset test isolation
