# Ensure consistent npm behavior across environments
engine-strict=true

# Fail fast on peer dependency conflicts
legacy-peer-deps=false

# Ensure lockfile is always respected
package-lock=true

# Use public npm registry
registry=https://registry.npmjs.org/
