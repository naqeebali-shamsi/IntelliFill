# IntelliFill Frontend Production Environment Variables
# Copy this file to .env.production and fill in your actual values
# DO NOT commit .env.production to version control

# ==============================================================================
# API CONFIGURATION
# ==============================================================================

# Backend API base URL
# This should point to your deployed backend server
# Include the /api path if your backend routes are prefixed with /api
# Example: https://api.intellifill.com/api
VITE_API_URL=

# ==============================================================================
# AUTHENTICATION MODE
# ==============================================================================

# Use backend authentication instead of Supabase direct auth
# Set to "true" for backend JWT auth (recommended for production)
# Set to "false" to use Supabase Auth directly from frontend
VITE_USE_BACKEND_AUTH=true

# ==============================================================================
# SUPABASE CONFIGURATION (Optional - only if VITE_USE_BACKEND_AUTH=false)
# ==============================================================================

# Supabase project URL
# Only required if using Supabase Auth directly (VITE_USE_BACKEND_AUTH=false)
# Example: https://abcdefghijklmnop.supabase.co
# VITE_SUPABASE_URL=

# Supabase anonymous/public API key
# Only required if using Supabase Auth directly (VITE_USE_BACKEND_AUTH=false)
# This is safe to expose in client-side code (it's the anon key, not service role)
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# VITE_SUPABASE_ANON_KEY=

# ==============================================================================
# MONITORING & ERROR TRACKING (Optional)
# ==============================================================================

# Sentry DSN for frontend error tracking
# Create a separate frontend project in Sentry for better error organization
# Example: https://abc123def456@o123456.ingest.sentry.io/7890124
# VITE_SENTRY_DSN=

# Sentry environment name
# VITE_SENTRY_ENVIRONMENT=production

# Sentry sample rate for performance monitoring (0.0 to 1.0)
# Lower values reduce data sent to Sentry (helps with quota management)
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.1

# Sentry sample rate for session replays (0.0 to 1.0)
# VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1

# Sentry sample rate for error session replays (0.0 to 1.0)
# VITE_SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# ==============================================================================
# ANALYTICS (Optional)
# ==============================================================================

# Google Analytics measurement ID
# Example: G-XXXXXXXXXX
# VITE_GA_MEASUREMENT_ID=

# Mixpanel project token
# VITE_MIXPANEL_TOKEN=

# PostHog API key and host
# VITE_POSTHOG_KEY=
# VITE_POSTHOG_HOST=https://app.posthog.com

# ==============================================================================
# FEATURE FLAGS
# ==============================================================================

# Enable/disable features in production
# VITE_ENABLE_KNOWLEDGE_BASE=true
# VITE_ENABLE_DOCUMENT_CHAT=true
# VITE_ENABLE_BULK_PROCESSING=true
# VITE_ENABLE_EXPORT=true

# ==============================================================================
# DEPLOYMENT INFORMATION (Optional)
# ==============================================================================

# Version number or git commit SHA for deployment tracking
# This can be injected by your CI/CD pipeline
# Example: VITE_APP_VERSION=1.2.3 or VITE_APP_VERSION=${GITHUB_SHA}
# VITE_APP_VERSION=

# Build timestamp (injected by CI/CD)
# VITE_BUILD_TIME=

# ==============================================================================
# RATE LIMITING & TIMEOUTS
# ==============================================================================

# API request timeout in milliseconds (default: 30000 = 30 seconds)
# VITE_API_TIMEOUT=30000

# Maximum file upload size in bytes (should match backend MAX_FILE_SIZE)
# Example: 10485760 = 10MB, 52428800 = 50MB
# VITE_MAX_FILE_SIZE=10485760

# Maximum number of files in bulk upload
# VITE_MAX_BULK_FILES=10

# ==============================================================================
# UI CONFIGURATION
# ==============================================================================

# Application name displayed in UI
# VITE_APP_NAME=IntelliFill

# Support email for contact forms
# VITE_SUPPORT_EMAIL=support@intellifill.com

# Documentation URL
# VITE_DOCS_URL=https://docs.intellifill.com

# ==============================================================================
# THIRD-PARTY INTEGRATIONS (Optional)
# ==============================================================================

# Stripe publishable key for payments
# VITE_STRIPE_PUBLISHABLE_KEY=

# Intercom app ID for customer support chat
# VITE_INTERCOM_APP_ID=

# ==============================================================================
# NOTES
# ==============================================================================
# 1. All Vite environment variables MUST be prefixed with VITE_ to be exposed
#    to the client-side code
# 2. These variables are embedded in the built JavaScript bundle and are
#    publicly accessible - NEVER put secrets here
# 3. Use your hosting platform's environment variable management:
#    - Vercel: Project Settings > Environment Variables
#    - Netlify: Site settings > Build & deploy > Environment
#    - Cloudflare Pages: Settings > Environment variables
# 4. Different platforms handle build-time vs runtime variables differently:
#    - Vercel/Netlify: Variables are injected at build time
#    - Consider using runtime config for values that change between deployments
# 5. Test your production build locally before deploying:
#    bun run build && bun run preview
# 6. For production deployment, ensure VITE_API_URL points to your production
#    backend and CORS_ORIGINS in backend includes your frontend URL
